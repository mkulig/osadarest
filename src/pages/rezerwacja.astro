---
import MainHeader from "../components/MainHeader.astro";
import { CONTACT_EMAIL, CONTACT_PHONE, CONTACT_PHONE_DISPLAY } from "../consts";
import Layout from "../layouts/Layout.astro";
---

<Layout
	title="Zarezerwuj pobyt"
	description="Lorem ipsum dolor sit amet"
>
    <section class="relative isolate bg-gradient-to-br from-zinc-50 via-white to-red-100 my-8 pb-20 rounded-2xl shadow-xs">
      <div class="container mx-auto max-w-3xl px-6 text-center">
          <MainHeader>
              <span slot="title">Rezerwacja pobytu w Osada REST</span>
              <span slot="description">
                  Zarezerwuj nocleg w naszych <strong>apartamentach w Tyliczu</strong>.
                  Skontaktuj się z nami telefonicznie lub mailowo – odpowiemy szybko i pomożemy wybrać najlepszą ofertę.
              </span>
          </MainHeader>

        <div class="mb-12 space-y-4">
          <p class="space-x-8 text-lg">
            📞 <a href={`tel:${CONTACT_PHONE}`} class="font-semibold text-[#ff6a00] hover:underline">{CONTACT_PHONE_DISPLAY}</a>
            ✉️ <a href={`mailto:${CONTACT_EMAIL}`} class="font-semibold text-[#ff6a00] hover:underline">{CONTACT_EMAIL}</a>
          </p>
        </div>

        <div class="flex flex-wrap justify-center gap-4">
          <a href={`tel:${CONTACT_PHONE}`}
             class="px-8 py-4 bg-gradient-to-l from-[#ff6a00] to-[#ee0979] text-2xl text-white font-semibold rounded-lg shadow hover:brightness-105  transition">
            Zadzwoń i zarezerwuj
          </a>
          <a href={`mailto:${CONTACT_EMAIL}`}
             class="rounded-lg bg-white border border-gray-300 px-8 py-4 text-lg font-semibold text-gray-700 hover:bg-gray-100 transition">
            Napisz do nas
          </a>
        </div>

        <p class="mt-12 text-base text-slate-500">
          Pobyty powyżej <strong>7 nocy</strong> objęte są rabatem – skontaktuj się, aby poznać szczegóły.
        </p>

      </div>
    </section>
</Layout>
